server:
  port: 8081
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/G-Pulenta-SI657/Config-data.git
          default-label: main
          clone-on-start: true

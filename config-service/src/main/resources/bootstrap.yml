server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/u201817461/Backend-Prueba.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          default-label: main
  security:
    user:
      name: root
      password: admin123
